/// Criterios de ordenación de la biblioteca (SPEC library-sorting).
///
/// `title` es el criterio predeterminado; `addedAt` ordena de más reciente a
/// más antigua (FR-SORT-003).
enum LibrarySortOption { title, artist, album, addedAt }
